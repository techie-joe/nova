extends ../../_basic/_html

block header
  +simpleHeader

mixin btns
  style.
    #btns button {
      margin: .1em;
      font-size: 0.8rem;
      vertical-align: top;
      line-height: inherit;
      min-height: 1.5rem;
    }
  ._container#btns: block

mixin boxa
  ._color_1._radius_5&attributes(attributes): block

mixin boxap
  +boxa._p._pa1px: block

mixin boxam(menu)
  +boxa._f: +menu_a2(menu)