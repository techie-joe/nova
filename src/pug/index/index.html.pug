extends _html

block append vars
  -
    pageid      = 'index'
    pageUrl     = siteUrl + '/index.html'
    title       = name
    description = 'Simple framework for websites and web applications.'

block append style
  style.
    #logo {
      display: block;
      text-align: center;
      font-size: 3rem;
      margin: 1em auto;
    }
    #maindesc {
      font-size:1rem;
      text-align: center;
      position: relative;
      top: -3em;
      margin:1em auto;
      font-weight: 400;
      color: inherit;
    }
  //- style.

block header
  header#_header
    ._color_1._pad: ._m_center._table(style="max-width:17em;")
      h1: +logo
      h2#maindesc|!{description}
    nav#_main_nav._color_1._flex
      ._f._pa_5: ._960
        +menues(['_a3m'],[
          ['starter','starter','Starter'],
          ['basic','basic','Basic'],
          ['lab','lab','Lab'],
        ])
      ._fit._pa_5: +changeThemeLink._a2m: +itext('_cicon')|☀
    hr._mt0

block article
  article._article
    //- section._container: pre.pre._scrollable: +pageData
    //- hr
    section._scrollable._container: pre.pre.
      Name         : !{name}
      Version      : !{version}

      Title        : !{title}
      Description  : !{description}

      URL          : !{pageUrl}
      URI          : !{homeUri} 

      Author       : !{author}
      Year         : !{year}
      LICENSE      : !{license}

      Last updated : !{today}
    |

block bottomRightLinks
  +bottomRightLinks('/')

//- block append script
  script.
    (()=>{
      const
        w = window,
        storage = localStorage,
        { log } = console;
    })();
  //- 