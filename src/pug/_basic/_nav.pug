mixin menu_a(v,classes)
  if typeof v === 'string'
    |!{v}
  else
    a(href=homeUri+v[1] class=classes.join(' ') title=v[3]) !{v[2]}

mixin menu_m1(links)
  nav._container
    each v in links
      - classes = ['_no_u'];
      - if(pageid === v[0]) classes = ['_no_color']
      +l
      +menu_a(v,classes)

mixin menu_a11(links)
  nav
    each v in links
      - classes = ['_a11'];
      - if(pageid === v[0]) classes.push('_active')
      +menu_a(v,classes)

mixin menu_a2(links)
  nav._container
    each v in links
      - classes = ['_a2'];
      - if(pageid === v[0]) classes.push('_active')
      +menu_a(v,classes)

//- --------------------------------------------------------------

//- pageid, uri, title

mixin main_nav
  hr._m0
  #_main_nav._color_2._flex
    ._f: +menu_a11([
      ['starter','starter','Starter'],
      ['basic','basic','Basic'],
      ['lab','lab','Lab'],
    ])
    ._fit: +changeThemeLink._a11: +itext('_cicon')|☀
  hr._m0

mixin buttons_menu
  +menu_a2([
    ['lab/new_buttons','lab/new_buttons.html','New Buttons'],
    ['basic/button','basic/button.html','Basic'],
  ])

mixin buttons_nav
  ._color_1._pa_125: +buttons_menu

